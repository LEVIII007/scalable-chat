{
    "name": "server",
    "private": true,
    "version": "1.0.0",
    "scripts": {
        "dev": "tsc-watch --onSuccess \"node dist/index.js\"",
        "start": "node dist/index",
        "build": "tsc -p ."
    },
    "devDependencies": {
        "tsc-watch": "^6.2.0",
        "typescript": "^5.6.3"
    },
    "dependencies": {
        "socket.io": "^4.8.1"
    }
}
